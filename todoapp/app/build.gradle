apply plugin: 'com.android.application'

android {
    compileSdkVersion 26
    defaultConfig {
        applicationId "com.example.android.architecture.blueprints.todoribs"
        minSdkVersion 16
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    // Support lib
    implementation deps.support.appcompat
    implementation deps.support.recyclerview
    implementation deps.support.annotations
    implementation deps.support.design

    // External deps
    implementation deps.uber.ribAndroid
    implementation deps.uber.ribScreenStackBase
    implementation deps.external.dagger
    implementation deps.external.rxbinding
    implementation deps.external.timber
    implementation deps.external.butterknife
    implementation deps.external.stetho
    implementation deps.arch.room
    implementation deps.arch.roomRxJava2

    annotationProcessor 'com.uber.rib:rib-compiler-test:0.9.1'
    annotationProcessor deps.apt.daggerCompiler
    annotationProcessor deps.apt.butterknife
    annotationProcessor deps.apt.room

    testImplementation 'com.uber.rib:rib-test-utils:0.9.1'
    testImplementation deps.test.junit
    androidTestImplementation deps.test.room
    androidTestImplementation 'com.android.support.test:runner:1.0.1'
    androidTestImplementation 'com.android.support.test.espresso:espresso-core:3.0.1'
}
